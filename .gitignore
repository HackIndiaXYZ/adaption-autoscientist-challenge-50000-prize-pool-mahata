# Large artifacts — host on HF/Kaggle, link from the cards. Don't commit to git.
*.safetensors
*.bin
*.gguf
*.pt
*.ckpt
*.tgz
*.tar.zst
checkpoints/
adapters/
wandb/
training/banglabridge-adapter/
training/*.tgz

# Full datasets — keep only scripts + a small sample.jsonl in git
data/raw/
data/full/
*.arrow
*.parquet
# (allow the small reviewer sample)
!data/sample.jsonl

# Adaptation run artifacts (large / regenerable)
*.log
data/adapt_run.log
data/full/

# Python
__pycache__/
*.pyc
.venv/
venv/
.env
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
